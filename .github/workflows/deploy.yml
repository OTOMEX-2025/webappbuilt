name: Deploy MindPal to EC2

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  APP_NAME: "MindPal"
  DOMAIN: "mindpal.otomexx.co.za"
  PORT: "7070"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        
    - name: Create .env file
      run: |
        echo "🔧 Creating .env file..."
        cat > .env << EOF
        MONGODB_URI=mongodb+srv://otomexinnovations:0TOMEX05@mindpal.0ikip.mongodb.net/?retryWrites=true&w=majority&appName=MindPal
        OPENAI_API_KEY=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
        GEMINI_API_KEY=AIzaSyD6t2SOEVLYWWwEqn4IrrsgT_DxxJi7eHg
        EMAIL_USER=otomexinnovations@gmail.com
        EMAIL_PASS=qmax vhuu lnvf usoj
        EOF
        echo "✅ .env file created"
        
    - name: Install dependencies
      run: |
        echo "📦 Installing dependencies..."
        npm install --silent --no-audit --no-fund
        echo "✅ Dependencies installed"
        
    - name: Build application
      run: |
        echo "🏗️ Building application..."
        echo "📁 Directory structure:"
        ls -la
        npm run build
        echo "✅ Build completed"
        
    - name: Verify build
      run: |
        echo "🔍 Verifying build..."
        if [ -d ".next" ]; then
          echo "✅ .next directory exists"
        else
          echo "❌ .next directory missing!"
          exit 1
        fi
        
    - name: Create deployment package
      run: |
        echo "📦 Creating deployment package..."
        mkdir -p deployment-package
        
        # Copy only essential files
        cp -r .next/ deployment-package/
        cp -r public/ deployment-package/ 2>/dev/null || true
        cp -r src/ deployment-package/ 2>/dev/null || true
        cp package.json deployment-package/
        cp package-lock.json deployment-package/
        cp next.config.js deployment-package/ 2>/dev/null || true
        cp ecosystem.config.js deployment-package/ 2>/dev/null || true
        cp .env* deployment-package/ 2>/dev/null || true
        
        echo "📁 Package contents:"
        ls -la deployment-package/
        
        # Create zip
        cd deployment-package && zip -r ../deploy-package.zip . && cd ..
        echo "📊 Package size: $(ls -lh deploy-package.zip | awk '{print $5}')"
        
    - name: Test SSH Connection
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: 22
        script: |
          echo "✅ SSH connection successful"
        
    - name: Transfer deployment package
      uses: burnett01/rsync-deployments@7.0.1
      with:
        switches: -avz --progress --no-perms
        path: deploy-package.zip
        remote_path: /tmp/
        remote_host: ${{ secrets.SERVER_HOST }}
        remote_user: ${{ secrets.SERVER_USERNAME }}
        remote_key: ${{ secrets.SSH_PRIVATE_KEY }}
        
    - name: Execute deployment
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: 22
        script_timeout: 20m
        script: |
          set -e
          echo "🚀 Starting deployment..."
          /home/ubuntu/scripts/deploy-app.sh "${{ env.APP_NAME }}" "${{ env.DOMAIN }}" "${{ env.PORT }}" "/tmp/deploy-package.zip"
          echo "✅ Deployment completed"
          
    - name: Cleanup
      run: |
        rm -f deploy-package.zip
        
    - name: Deployment success
      run: |
        echo "🎉 DEPLOYMENT SUCCESSFUL!"
        echo "🌐 Website: https://${{ env.DOMAIN }}"
        echo "📱 App: ${{ env.APP_NAME }}"
        echo "🚪 Port: ${{ env.PORT }}"
